/**
 * Responsive Nav Bar
 *
 * Build a navigation bar that adapts between desktop and mobile layouts.
 *
 * Requirements:
 * 1. Desktop (>768px): horizontal nav links displayed in a row using flexbox.
 * 2. Mobile (<=768px): links hidden behind a hamburger menu button.
 * 3. Clicking the hamburger toggles a vertical dropdown menu.
 * 4. Smooth slide-down animation on the mobile menu.
 * 5. Clicking a nav link closes the mobile menu.
 * 6. Clicking outside the menu closes it.
 *
 * Hints:
 * - Use a CSS media query at 768px breakpoint.
 * - flexbox for horizontal layout, column direction for mobile dropdown.
 * - transform: translateY or max-height transition for slide animation.
 *
 * Time target: 15 minutes.
 */

import "./NavBar.css";

export const NavBar = () => {
  return <div>NavBar</div>;
};
