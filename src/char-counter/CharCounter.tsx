/**
 * Character Counter Textarea
 *
 * Requirements:
 * 1. A textarea with a max character limit of 200.
 * 2. Below it, show "X / 200 characters" updating as the user types.
 * 3. When the user exceeds 180 characters, the counter text turns red.
 * 4. A "Clear" button resets the textarea and counter.
 * 5. The textarea should not accept more than 200 characters.
 *
 * Time target: 5 minutes.
 */

import styles from "./CharCounter.module.css";

export const CharCounter = () => {
  return <div>Character Counter</div>;
};
